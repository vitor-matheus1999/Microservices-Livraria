info:
  name: Find Author By ID
  type: http
  seq: 6

http:
  method: GET
  url: "{{url}}/author/4"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
